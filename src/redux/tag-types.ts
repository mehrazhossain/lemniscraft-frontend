export enum tagTypes {
  user = "user",
  service = "service",
  booking = "booking",
  auth = "auth",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.service,
  tagTypes.booking,
  tagTypes.auth,
];
